export default function ( context, notctx) {
  // const {router} = context.app

  // router.onReady(() => {
  //   const matchedComponents = router.getMatchedComponents(context.app.context.route);
  //   console.log(matchedComponents[0].name)
  //   const promises = matchedComponents.map(
  //   component => {
  //     console.log(component)
  //     return component.asyncData &&
  //     component.asyncData().then(data => ({ data, component }))
  //   }

  // )
// }
  
  // )
  
  // res is the Node.js http response object

  // next is a function to call to invoke the next middleware
  // Don't forget to call next at the end if your middleware is not an endpoint!
  // next()
}